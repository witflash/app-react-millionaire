import { memo } from 'react';

import styles from './FinalResults.module.scss';

function FinalResults() {
  return <div className={styles.block}>FinalResults</div>;
}

export default memo(FinalResults);
